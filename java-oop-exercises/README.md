# Java OOP Exercises

Esta pasta contém exercícios de **Programação Orientada a Objetos (OOP) em Java**, organizados por tópicos para facilitar o estudo e a prática dos principais conceitos de OOP.

## Estrutura

### 0. OOP Básico
- **00-oop-basic**  
  Exercícios introdutórios sobre classes, objetos, atributos e métodos.

### 1. Classes e Objetos
- **01-person-exercise**  
  Criação de uma classe `Person` com atributos, construtor e métodos getters/setters.
- **02-circle-exercise**  
  Classe `Circle` com cálculo de área e perímetro.
- **03-car-exercise**  
  Classe `Car` com atributos e método para calcular a idade do carro.

### 2. Herança
- **04-animal-inheritance**  
  Classe base `Animal` e subclasses como `Dog` e `Cat`, com sobrescrita de métodos.
- **05-employee-inheritance**  
  Classe `Employee` e subclasses com diferentes formas de cálculo de salário.
- **06-shape-inheritance**  
  Classe abstrata `Shape` e subclasses com implementação específica de área.

### 3. Encapsulamento
- **07-encapsulation-bank**  
  Classe `BankAccount` com controle de acesso ao saldo.
- **08-encapsulation-product**  
  Classe `Product` com validação de preço.
- **09-encapsolution-user**  
  Classe `User` com atributos privados e método de autenticação.

### 4. Polimorfismo
- **10-polymorphism-animal**  
  Uso de polimorfismo com uma lista de animais e chamadas de métodos sobrescritos.
- **11-polymorphism-printable**  
  Implementação de uma interface `Printable` em diferentes classes.
- **12-polymorphism-payments**  
  Uso de polimorfismo para lidar com diferentes tipos de pagamento ou contas.

## Objetivo

O objetivo destes exercícios é:

- Consolidar os fundamentos de OOP em Java
- Praticar boas práticas de organização de código
- Entender como classes se relacionam entre si
- Preparar a base para projetos maiores e mais complexos

📌 Todos os exercícios são focados em aprendizado e prática, sem dependência de interface gráfica ou frameworks externos.
